---
"@useprint/preview": patch
---

Declare missing direct dependencies for `@useprint/preview` so fresh `init-doc` installs can resolve the preview app font imports and bundled test helpers without relying on monorepo-only root packages.
