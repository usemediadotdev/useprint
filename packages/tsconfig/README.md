# tsconfig

Shared TypeScript presets for the Useprint workspace.

## Included presets

- `base.json`
- `react-library.json`
- `nextjs.json`

## Notes

This package is mainly an internal workspace utility. It keeps TypeScript settings consistent across libraries and apps in the monorepo.
